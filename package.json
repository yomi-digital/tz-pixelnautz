{
  "name": "tz-astronaut-ai",
  "description": "FA2 non-fungible tokens on Tezos",
  "version": "2.0.10",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn lerna run build",
    "sandbox": "yarn start-sandbox",
    "compile-contract": "ts-node ./scripts/compile-contract.ts",
    "deploy": "ts-node ./scripts/compile-contract && ts-node ./scripts/deploy.ts",
    "addminter": "ts-node ./scripts/addminter.ts",
    "mint": "ts-node ./scripts/mint.ts",
    "minted": "ts-node ./scripts/minted.ts",
    "freeze": "ts-node ./scripts/freeze.ts",
    "unfreeze": "ts-node ./scripts/unfreeze.ts"
  },
  "devDependencies": {
    "@types/configstore": "^5.0.1",
    "configstore": "^6.0.0",
    "lerna": "^4.0.0"
  },
  "dependencies": {
    "@taquito/signer": "^11.1.0",
    "minimist": "^1.2.5"
  }
}
